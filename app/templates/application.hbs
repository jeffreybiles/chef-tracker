<h1>Chef Tracker</h1>
<ul>
  {{#each chefs as |chef|}}
    {{#if chef.isHere}}
      <li class="is-here">{{chef.name}}</li>
    {{else}}
      <li class="not-here">{{chef.name}}</li>
    {{/if}}
  {{/each}}
</ul>
