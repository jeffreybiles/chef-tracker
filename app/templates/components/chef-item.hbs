<li class="{{if chef.isHere '' 'not-here'}}">
  {{chef.name}}
  <button {{action 'toggleOpen'}}>{{if isOpen 'v' '>'}}</button>
  {{#if isOpen}}
    <br>
    <button {{action 'togglePresent' chef}}>{{if chef.isHere 'Exit' 'Enter'}}</button>
  {{/if}}
</li>
