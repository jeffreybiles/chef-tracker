<h1>Login</h1>

<div class="field">
  <label>Email</label>
  {{input value=email}}
</div>
<div class="field">
  <label>Password</label>
  {{input value=password type="password"}}
</div>

<button {{action 'login'}}>Login</button>
